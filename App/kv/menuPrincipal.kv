#:kivy 2.1.0

<MenuPrincipalScreen>:

    BoxLayout:
        orientation: "vertical"
        padding: 40
        spacing: 25
        size_hint_y: None
        height: root.height

        Label:
            text: "Hotel Bruno R&F"
            font_size: 40
            bold: True
            color: 0.1, 0.1, 0.1, 1

        StyledButton:
            text: "Mis Reservas"
            on_press: root.abrir_mis_reservas(self)

        StyledButton:
            text: "Hacer una reserva"
            on_press: root.abrir_reservas(self)

        StyledButton:
            text: "Mi Cuenta"
            on_press: root.abrir_mi_cuenta(self)

        StyledButton:
            text: "Cerrar sesión"
            on_press: root.cerrar_sesion(self)